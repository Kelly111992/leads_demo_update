/**
 * WebhookProcessor - Supabase-based version
 * This component doesn't render anything visible.
 * Server-side (server.ts) handles all webhook processing directly into Supabase.
 * This component is kept as a no-op for backward compatibility.
 */
export function WebhookProcessor() {
  return null;
}
